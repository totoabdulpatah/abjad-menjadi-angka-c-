#include <stdio.h>
// #include <conio.h>  // Ini dihapus karena kita tidak menggunakan fungsinya

int main()
{
    int u = 8;
    char t = 'H';

    // clrscr();  // Dihapus karena tidak digunakan dalam standar C

    printf("%c merupakan abjad yang ke - %d\n", t, u);

    // getch();  // Dihapus karena tidak digunakan dalam standar C
    return 0;
}
